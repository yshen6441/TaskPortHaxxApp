name: Build Unsigned IPA

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Select Xcode 16.1
      run: |
        sudo xcode-select -s /Applications/Xcode_16.1.app/Contents/Developer
        xcodebuild -version

    - name: Find .xcodeproj
      id: proj
      run: |
        PROJ=$(find . -name "*.xcodeproj" -type d | head -n1)
        if [ -z "$PROJ" ]; then
          echo "No .xcodeproj found!"
          exit 1
        fi
        echo "project=$PROJ" >> $GITHUB_OUTPUT

    - name: Get Scheme
      id: scheme
      run: |
        SCHEME=$(xcodebuild -list -project "${{ steps.proj.outputs.project }}" | \
          awk '/Schemes:/,/^$/ {if(NR>1) print}' | head -1 | xargs)
        echo "scheme=$SCHEME" >> $GITHUB_OUTPUT

    - name: Build Binary (No Signing)
      run: |
        xcodebuild \
          -project "${{ steps.proj.outputs.project }}" \
          -scheme "${{ steps.scheme.outputs.scheme }}" \
          -configuration Release \
          -sdk iphoneos \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGN_IDENTITY="" \
          PROVISIONING_PROFILE="" \
          DEVELOPMENT_TEAM="" \
          -quiet \
          build

    - name: Find Executable
      id: bin
      run: |
        BIN=$(find build -type f -perm +111 -size +10k | head -n1)
        if [ -z "$BIN" ]; then
          echo "No binary found! Listing build:"
          ls -la build || true
          exit 1
        fi
        echo "Found: $BIN"
        file "$BIN"
        echo "path=$BIN" >> $GITHUB_OUTPUT

    - name: Create Minimal IPA
      run: |
        mkdir -p build/Payload/TaskPortHaxxApp.app
        
        cp "${{ steps.bin.outputs.path }}" build/Payload/TaskPortHaxxApp.app/TaskPortHaxxApp
        
        cat > build/Payload/TaskPortHaxxApp.app/Info.plist << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleExecutable</key><string>TaskPortHaxxApp</string>
    <key>CFBundleIdentifier</key><string>com.yshen6441.taskporthaxx</string>
    <key>CFBundleName</key><string>TaskPortHaxxApp</string>
    <key>CFBundleVersion</key><string>1</string>
    <key>CFBundleShortVersionString</key><string>1.0</string>
    <key>DTPlatformName</key><string>iphoneos</string>
    <key>MinimumOSVersion</key><string>12.0</string>
</dict>
</plist>
EOF

        cd build
        zip -r9 ../TaskPortHaxxApp-unsigned.ipa Payload
        cd ..
        ls -la TaskPortHaxxApp-unsigned.ipa

    - name: Upload IPA
      uses: actions/upload-artifact@v4
      with:
        name: TaskPortHaxxApp-unsigned-ipa
        path: TaskPortHaxxApp-unsigned.ipa
